# Copy this to .env and fill in your values
# Never commit .env to git

# ── Omada controller ──────────────────────────────
OMADA_BASE_URL=https://35.184.66.225:8043
OMADA_CONTROLLER_ID=Omada Network_Clouddle        # Omada web UI → Settings → Controller ID
OMADA_SITE_ID=default                       # MSP site ID, or "default" for single-site
OMADA_CLIENT_ID=26950efc7d844b90bda24a5f969fea27        # Omada web UI → Settings → Open API
OMADA_CLIENT_SECRET=a6191e6e6ccb42a6888f4539f4ff7512
OMADA_PPSK_PROFILE_ID=https://35.184.66.225:8043/#ppsk  # Omada web UI → Wireless → PPSK → copy from URL
OMADA_MODE=local                             # "local" for self-hosted, "cloud" for Omada Cloud

# ── Firebase ──────────────────────────────────────
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
